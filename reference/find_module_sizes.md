# Determine module sizes

Determine module sizes

## Usage

``` r
find_module_sizes(module, n_modules)
```

## Arguments

- module:

  Vector of module indices

- n_modules:

  Total number of modules

## Value

A named vector containing the name of the module (its index or
`"Noise"`) and the number of elements in that module
