# Return the number of final configurations

Returns the number of final configurations per penalization parameter in
an scRegClust object.

## Usage

``` r
get_num_final_configs(fit)
```

## Arguments

- fit:

  An object of class `scRegClust`

## Value

An integer vector containing the number of final configurations for each
penalization parameter.
